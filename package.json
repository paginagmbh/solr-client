{
  "name": "solr-client",
  "version": "2.4.1",
  "description": "Client for Apache Solr",
  "main": "./dist/solr.js",
  "license": "BSD-2-Clause",
  "bin": {
    "solr-loader": "./bin/solr-loader.js"
  },
  "engines": {
    "node": ">=8.6"
  },
  "dependencies": {
    "fs-extra": "^6.0.1",
    "globby": "^8.0.1",
    "minimist": "^1.2.0",
    "netrc": "^0.1.4",
    "request": "^2.85.0"
  },
  "devDependencies": {
    "coffeescript": "^2.3.1"
  },
  "scripts": {
    "prepare": "coffee --compile --map --output dist/ lib/",
    "provision:vm": "cd provisioning && vagrant up"
  }
}
